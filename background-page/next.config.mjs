/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  // Turbopack configuration (empty because our shaders are already in .js format)
  turbopack: {},
};

export default nextConfig;
